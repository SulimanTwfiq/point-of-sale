import React from "react"
import DishCard from "./DishCard"

const DishesList = ({ setCart }) => {
  return (
    <div className="grid grid-cols-3 gap-7">
      <DishCard onClick={() => {}} />
    </div>
  )
}

export default DishesList
